# FRACDASH Devlog

## 2026-03-15

- Decided to upgrade the physics lane from generic graph-dynamical diagnostics to a V1 mixed exact/statistical target suite with direct physics-facing surrogate laws.
- Fixed the target vocabulary in `PHYSICS_INVARIANT_TARGETS.md`: exact laws are now source-charge conservation, source-parity preservation, locality bounds, forward-cone bounds, and cycle-distance nonincrease; statistical laws are perturbation stability, geodesic-like flow, curvature-like concentration, and defect attraction.
- Extended `scripts/physics_invariant_analysis.py` to emit `physics_target_suite_v1` in every invariant artifact while preserving the existing candidate-invariant output.
- Extended `scripts/check_physics_invariant_targets.py` to enforce the new V1 suite on the locked `physics2..physics8` set; the refreshed gate still passes.
- Refreshed invariant artifacts for `physics2..physics8` plus `physics19`. The locked set now explicitly records exact source-charge conservation (`R1`,`R2`), exact source-parity preservation, locality bound `max_changed_registers <= 3`, forward-cone bound `max_successor_hamming <= 5`, and cycle-distance nonincrease `1.0`.
- Corrected the geometry surrogates to ignore non-cycle-reachable neighbors when comparing local cycle distance. This removes the previous `distance_to_cycle = -1` bias from geodesic-like and curvature-like measurements.
- Re-ran `physics18` and `physics19` under the corrected surrogate. On an apples-to-apples basis they are currently equivalent on deterministic recurrent metrics (`274` deterministic edges, `geodesic_like_flow.near_min_ratio = 0.9094`, `mean_neighbor_spread = 1.8258`, `defect_nonincrease_ratio = 0.6496`), which means the `physics19` tail handoff is not yet changing the deterministic graph seen by the V1 analyzer.
- Defined the next two implementation targets from that result:
  - `physics20` should widen the deterministic recurrent graph from the `physics18/19` baseline without breaking the exact V1 laws.
  - the invariant analyzer should grow an observable-surrogate block so shell/interior occupancy, re-entry flow, and source-defect coupling are reported directly.
- Implemented `physics20` in `scripts/agdas_bridge.py` as a boundary positive-phase discharge rule, added `scripts/run_agdas_physics20_phase2.sh`, and captured `benchmarks/results/2026-03-15-agdas-physics20-phase2.json`.
- `physics20` is the first post-correction widening branch that changes the deterministic recurrent graph seen by the analyzer: `274 -> 301` deterministic edges, `359 -> 386` phase edges, `455 -> 428` terminal states, and improved corrected geometry/defect surrogates while keeping the exact V1 laws intact.
- Added `observable_surrogates` to `scripts/physics_invariant_analysis.py`; invariant artifacts now report region occupancy, action-phase occupancy, re-entry flow, latch alignment, and source-defect coupling.
- Fixed a template-ID anchoring bug in `scripts/check_physics_invariant_targets.py` where `physics20` could be misread as `physics2`; the lock gate now passes again after the fix.
- Declared the next split physics round before implementation:
  - `physics21` on the same 6-register carrier should target the first direct `boundary -> interior` re-entry without breaking the exact V1 laws.
  - `carrier8_physics1` should start a separate physics-local 8-register branch with `R7` boundary-return memory and `R8` transport/debt memory so later interpretation can separate return type from deferred transport load.
- Implemented `physics21` in `scripts/agdas_bridge.py` as a direct mid-severity boundary re-entry rule, added `scripts/run_agdas_physics21_phase2.sh`, and captured `benchmarks/results/2026-03-15-agdas-physics21-phase2.json`.
- `physics21` is now the strongest 6-register exploratory branch: `301 -> 310` deterministic edges, `386 -> 395` phase edges, `428 -> 419` terminal states, first direct `boundary_to_interior = 9`, corrected `geodesic_like_flow.near_min_ratio = 0.9207`, and exact V1 laws still intact.
- Added the exploratory 8-register carrier via `scripts/agdas_physics8_state.py`, validated full `3^8 = 6561` encode/decode round-trip, added `scripts/agdas_physics8_experiments.py`, and captured `benchmarks/results/2026-03-15-agdas-carrier8-physics1-phase2.json`.
- Captured the carrier-definition artifact at `benchmarks/results/2026-03-15-agdas-carrier8-state.json`.
- Extended `scripts/physics_invariant_analysis.py` with carrier-aware runtime loading, bounded active-subset analysis for the 8-register branch, and the new `boundary_return_profile` / `transport_debt_profile` observable blocks.
- Captured `benchmarks/results/2026-03-15-physics-invariants-physics21.json` and `benchmarks/results/2026-03-15-physics-invariants-carrier8_physics1.json`.
- The 8-register branch is structurally useful but not promotion-ready: it exposes explicit memory observables, but its first branch-local invariant pass does not preserve the 6-register locality/forward-cone signature and is therefore exploratory only.
- Added `MONSTER10WALK_CANONICAL.md` to freeze canonical 10-walk semantics (vertex order, edge rule, derivation match rule, strict lock gate).
- Added `scripts/freeze_monster10walk_canonical.py` and captured `benchmarks/results/2026-03-15-monster10walk-canonical.json`.
- Expanded `scripts/freeze_monster10walk_canonical.py` to require transition-witnessed canonical edges and dual-template lock support (`physics8|physics9`) by default.
- Strict lock now passes under the dual-template gate: canonical chain/rank proxy `4`, and both `physics8` and `physics9` provide full canonical support (`9/9`) in direct and reachability relations.
- Added `scripts/quarantine_monsterlean_claims.py` and captured `benchmarks/results/2026-03-15-monsterlean-claim-status.json` plus `.md` for file-by-file Lean theorem quarantine.
- Expanded the quarantine report with a prioritized closure queue and per-file blocker names for `MonsterWalk.lean`, `MonsterWalkPrimes.lean`, `MonsterWalkRings.lean`, and `BottPeriodicity.lean`.
- Closed all four prioritized files by removing `axiom`/`sorry` placeholders and replacing them with executable, narrowed statements (`native_decide` or direct constructive proofs).
- Refreshed quarantine artifacts: current summary over `monster/MonsterLean/*.lean` is `94` files scanned, `35` closed, `59` quarantined, with prioritized queue now fully closed.
- Added `scripts/extract_monsterlean_10walk.py` to parse constants from `monster/MonsterLean/BottPeriodicity.lean` into a candidate 10-walk dataset with explicit edge-policy diagnostics.
- Captured `benchmarks/results/2026-03-15-monsterlean-10walk-extract.json`; sequence-only edges with the observed degeneracy table produce longest strict descending chain `4`, while adding periodic/clue edges raises it to `5`.
- Added `physics9` as the next constrained refinement in `scripts/agdas_bridge.py` and wired execution through `scripts/agdas_physics_experiments.py` plus `scripts/run_agdas_physics9_phase2.sh`.
- Captured `benchmarks/results/2026-03-15-agdas-physics9-phase2.json`; compared to `physics8`, it raises edges from `222` to `228` while keeping longest chain `13`, cyclic starts `132`, action-rank increases `27`, and timeout count `0`.
- Added `physics10` as the next constrained refinement in `scripts/agdas_bridge.py`, wired it into `scripts/agdas_physics_experiments.py`, and added `scripts/run_agdas_physics10_phase2.sh`.
- Captured `benchmarks/results/2026-03-15-agdas-physics10-phase2.json`; compared to `physics9`, it raises edges from `228` to `229`, keeps longest chain at `13`, raises cyclic starts from `132` to `146`, keeps action-rank increases flat at `27`, and keeps timeout count at `0`.
- Added `physics11` as the next constrained refinement in `scripts/agdas_bridge.py`, wired it into `scripts/agdas_physics_experiments.py`, and added `scripts/run_agdas_physics11_phase2.sh`.
- Captured `benchmarks/results/2026-03-15-agdas-physics11-phase2.json`; compared to `physics10`, it raises edges from `229` to `256`, keeps longest chain at `13`, raises cyclic starts from `146` to `161`, keeps action-rank increases flat at `27`, and keeps timeout count at `0`.
- Captured `benchmarks/results/2026-03-15-physics-invariants-physics11.json`; `distance_to_cycle` remains strictly non-increasing (`nonincrease_ratio=1.0`, `increase_ratio=0.0`) on deterministic edges.
- Added `physics12` in `scripts/agdas_bridge.py` and `scripts/run_agdas_physics12_phase2.sh`, then captured `benchmarks/results/2026-03-15-agdas-physics12-phase2.json`; this widened breadth (`274` edges) while preserving constrained bounds (`chain=13`, `cycle=161`, `increases=27`, `timeouts=0`).
- Added `physics13` in `scripts/agdas_bridge.py` and `scripts/run_agdas_physics13_phase2.sh`, then captured `benchmarks/results/2026-03-15-agdas-physics13-phase2.json`; this achieved the depth target (`longest_chain=14`) while preserving constrained bounds (`increases=27`, `timeouts=0`).
- Captured `benchmarks/results/2026-03-15-physics-invariants-physics12.json` and `benchmarks/results/2026-03-15-physics-invariants-physics13.json`; deterministic `distance_to_cycle` non-increase remains intact.
- Added `physics14` in `scripts/agdas_bridge.py` and `scripts/run_agdas_physics14_phase2.sh`, then captured `benchmarks/results/2026-03-15-agdas-physics14-phase2.json`; this improved constrained recurrence to `302` edges and `194` cyclic starts while preserving `longest_chain=14`, `increases=27`, and `timeouts=0`.
- Captured `benchmarks/results/2026-03-15-physics-invariants-physics14.json`; deterministic `distance_to_cycle` non-increase remains intact (`nonincrease_ratio=1.0`, `increase_ratio=0.0`).
- Added `physics15` in `scripts/agdas_bridge.py` and `scripts/run_agdas_physics15_phase2.sh`, then captured `benchmarks/results/2026-03-15-agdas-physics15-phase2.json`; this increased constrained depth to `longest_chain=22` while preserving `cycle=194`, `increases=27`, and `timeouts=0`.
- Captured `benchmarks/results/2026-03-15-physics-invariants-physics15.json`; deterministic `distance_to_cycle` non-increase remains intact (`nonincrease_ratio=1.0`, `increase_ratio=0.0`).
- Added `physics16` in `scripts/agdas_bridge.py` and `scripts/run_agdas_physics16_phase2.sh`, then captured `benchmarks/results/2026-03-15-agdas-physics16-phase2.json`; this reduced terminal states to `502` and improved recurrence to `cycle=227` while preserving `longest_chain=22`, `increases=27`, and `timeouts=0`.
- Added `physics17` in `scripts/agdas_bridge.py` and `scripts/run_agdas_physics17_phase2.sh`, then captured `benchmarks/results/2026-03-15-agdas-physics17-phase2.json`; this raised constrained depth to `longest_chain=29` while preserving `cycle=227`, `increases=27`, and `timeouts=0`.
- Captured `benchmarks/results/2026-03-15-physics-invariants-physics16.json` and `benchmarks/results/2026-03-15-physics-invariants-physics17.json`; deterministic `distance_to_cycle` non-increase remains intact.
- Added `physics18` in `scripts/agdas_bridge.py` and `scripts/run_agdas_physics18_phase2.sh`, then captured `benchmarks/results/2026-03-15-agdas-physics18-phase2.json`; this reduced terminal states to `475` and improved recurrence to `cycle=254` while preserving `longest_chain=29`, `increases=27`, and `timeouts=0`.
- Captured `benchmarks/results/2026-03-15-physics-invariants-physics18.json`; deterministic `distance_to_cycle` non-increase remains intact (`nonincrease_ratio=1.0`, `increase_ratio=0.0`).
- Extended `scripts/physics_invariant_analysis.py` and `scripts/ablate_prime_triplets.py` to accept `--template-set physics9`, then captured `benchmarks/results/2026-03-15-physics-invariants-physics9.json` and `benchmarks/results/2026-03-15-prime-triplet-ablation-physics9.json`.
- Re-ran `scripts/check_physics_invariant_targets.py --json`; the locked `physics2..physics8` gate still passes after the `physics9` integration.
- Added `scripts/ablate_prime_triplets.py` to run deterministic q-map ablations over prime triplets and compare `effective_dimension` / `descending_chain_height` against the `physics8` transition set.
- Captured `benchmarks/results/2026-03-15-prime-triplet-ablation.json`; this first pass does not isolate a `{47,59,71}`-specific rank-4 signal in the projection-only sweep.
- Added `PHYSICS_INVARIANT_TARGETS.md` to lock the concrete physics-lane target table as explicit numbers plus relationship checks instead of open-ended narrative goals.
- Added `scripts/check_physics_invariant_targets.py` as a deterministic gate that validates the latest `physics2..physics8` phase/invariant artifacts (Lyapunov nonincrease, controlled-hybrid growth, long-tail resolution, and `physics4` overconstraint signature).
- Captured `benchmarks/results/2026-03-15-physics-invariant-target-check.json`; current artifact passes all checks.
- Reworked `scripts/derive_rank4_dataset.py` into a dual-derivation pipeline (`walk_partition` + `scc_condensation`) with canonical best-of-two selection, keep-previous tiebreak support, and explicit lock-gate metadata.
- Reworked `scripts/run_rank4_diagnostics.py` to compare both derivations in one report and split strict behavior into `--strict-stable` vs `--strict-lock`.
- Updated `scripts/run_rank4_discriminators.py` to consume the selected canonical derivation by default (or an explicit derivation override).
- Added `scripts/run_rank4_canonical_gpu_parity.py` to run a lock-gated GPU parity batch on canonical basin representative states; current artifact correctly reports `skipped` because lock gate is not yet satisfied.
- Captured updated artifacts: `2026-03-15-rank4-dataset.json`, `2026-03-15-rank4-diagnostics.json`, `2026-03-15-rank4-discriminators.json`, `2026-03-15-rank4-discriminators.md`, and `2026-03-15-rank4-canonical-gpu-parity.json`.

## 2026-03-13

- Initialized repo memory around the DASHI-to-FRACTRAN objective.
- Resolved the primary conversation `Dashi and FRACTRAN Analysis`.
- Resolved and refreshed `Modern FRACTRAN Implementations`; noted the live thread was newer than the DB copy.
- Recorded the decision to treat `fractran/` as the CPU baseline and `../dashiCORE` as the external GPU helper source.
- Added `fractran-bench` and adjusted `fractran/build.sh` to compile dynamically on this machine.
- Added `fractran/src/Compiled.hs` as an explicit seam for compiled exponent-vector execution.
- Saved first benchmark artifacts under `benchmarks/results/`.
- Expanded `fractran-bench` with scenario presets, logical-step mode, repeats, and JSONL output.
- Added `benchmarks/run_cpu_matrix.sh` and `benchmarks/summarize_cpu_matrix.py`.
- Ran the CPU matrix across `mult_smoke`, `primegame_small`, `primegame_medium`, and `primegame_large`.
- Verified parity for `reg`, `frac-opt`, and `compiled` across the matrix.
- Recorded the benchmark-backed next target: CPU LUT/divisibility-mask work.
- Hardened the benchmark contract by adding explicit checkpoint policy semantics.
- Fixed `mult_smoke` to use its real exact logical-step target (`2`) so the matrix no longer contains invalid runs.
- Re-ran the canonical matrix and confirmed the corrected artifact set still points to LUT/divisibility-mask CPU work.
- Implemented a binary-threshold LUT path in `fractran/src/Compiled.hs` and exposed it as the `lut` engine in `fractran-bench`.
- Fixed a strictness bug in the LUT runner so benchmark checkpoints stream lazily instead of forcing the entire execution.
- Verified that `lut` matches exact-step parity on `mult_smoke` and `primegame_*`, and that it rejects `hamming` as incompatible because `hamming` requires denominator thresholds greater than `1`.
- Re-ran the canonical matrix with `lut` included and recorded the result that `frac-opt` remains the practical CPU baseline.
- Parked LUT as a completed side experiment and pivoted the active optimization track back to compiled-path tuning.
- Added explicit CPU-to-GPU handoff criteria so `../dashiCORE` integration does not begin before CPU work has genuinely flattened out.
- Ported `frac-opt`-style rule-order narrowing into the compiled evaluator and reran the canonical matrix without `lut` in the active set.
- Verified that parity still holds and that the updated summary now routes to `continue compiled-path tuning`.
- Added a compiled-specific benchmark summary path that keeps compiled states as exponent vectors until checksum/hash evaluation instead of decoding every step to `IntMap`.
- Re-ran the canonical matrix and measured `compiled` ahead of `frac-opt` on `primegame_small`, `primegame_medium`, and `primegame_large`.
- Installed the host GHC static profiling package and updated `fractran/build.sh --profile` to use static profiling on this machine.
- Ran real cost-centre profiling on `compiled` for `primegame_medium` and `primegame_large`, with RTS comparison runs against `frac-opt`.
- Confirmed that compiled profiling is dominated by benchmark-side `unfExpVec`/checksum work, with `applyRule` the largest remaining engine-side hotspot and rule matching smaller.
- Rewrote `applyRule` to use element-wise zipping over the compiled state and rule delta instead of repeated array indexing in the hot path.
- Added direct integer-value tracking to the compiled benchmark trace so checksum generation no longer reconstructs every emitted state with `unfExpVec`.
- Re-ran the canonical matrix and confirmed `compiled` now leads `frac-opt` on `primegame_small`, `primegame_medium`, and `primegame_large`.
- Declared the compiled path the active exact-step CPU baseline and pivoted the next milestone toward the minimal GPU path.
- Audited `../dashiCORE` for actual FRACDASH reuse candidates and documented a non-duplicating integration boundary centered on `gpu_common_methods.py`, `gpu_vulkan_dispatcher.py`, `gpu_vulkan_backend.py`, `gpu_vulkan_adapter.py`, and `gpu_vulkan_gemv.py`.
- Documented the rule that generic helpers should be upstreamed back into `dashiCORE`, while FRACTRAN-specific kernels remain local to FRACDASH.
- Added `gpu/dashicore_bridge.py` as the FRACDASH-local import seam for `../dashiCORE`.
- Added `scripts/check_dashicore_reuse.py` to prove by-reference module import, shared shader asset resolution, and adapter passthrough without copying CORE code.
- Added `GPU_CONTRACT.md` and `gpu/fractran_layout.py` to define the first exact-step FRACTRAN GPU contract as dense exponent vectors plus per-rule thresholds/deltas.
- Added `scripts/check_fractran_gpu_layout.py` to compare that contract against the Haskell `compiled` baseline on `mult_smoke` and `primegame_small`.
- Added `gpu_shaders/fractran_step.comp` and `gpu/vulkan_fractran_step.py` to implement the first real one-step FRACTRAN Vulkan kernel path.
- Added `scripts/check_fractran_vulkan_step.py` and verified host-GPU parity against the dense CPU step on `mult_smoke` and `primegame_small`.
- Expanded the Vulkan step path to flattened batched state buffers and verified host-GPU batch parity, including per-state selected-rule and halted metadata.
- Extended the batched Vulkan runner to keep buffers resident across repeated exact steps and verified final-state parity against the dense CPU contract.
- Reworked the resident multi-step runner to use ping-pong descriptor sets and a single recorded command buffer submission, then revalidated host-GPU parity.
- Added `scripts/benchmark_fractran_gpu.py`, saved the first routing benchmark artifact, and measured the resident GPU path ahead of the dense CPU contract on `primegame_small` for batch sizes `32`, `128`, and `512` at `32` exact steps.
- Expanded the routing matrix across `mult_smoke` and `primegame_small` for multiple batch/step regimes, saved `2026-03-13-gpu-routing-matrix.json`, and derived the first conservative host routing rule.
- Added the `paper_smoke` routing matrix (`2026-03-13-gpu-routing-paper.json`), confirming the same GPU-favored threshold at `batch_size = 32`, `steps >= 8`.
- Documented the minimal DASHI signed-register state space and FRACTRAN encoding in `DASHI_STATE.md`.
- Extended the routing matrix with `hamming_smoke` plus the `batch_size = 16..64`, `steps = 4..16` band so the middle region now resolves into a deterministic rule stored in `benchmarks/results/2026-03-13-gpu-routing-matrix-extended.json`.
- Added `scripts/toy_dashi_transitions.py` to enumerate a toy `{-1,0,+1}^4` transition set, encode/decode the state primes, and output the FRACTRAN fractions that are the first Phase 2 CORE experiment artifact.
- Extended the toy script so it now emits basin graph summaries, reports the longest monotone chain from the canonical start state, and walks the full `3^4=81` encoded state space to kick-off the fixed-prime/basin explorations.
- Added a fixed-prime-vs-explicit-FRACTRAN dynamics comparison across all 81 states and deterministic basin partition summaries; the current artifact shows zero comparison mismatches and 72 basin attractors under current transition-ordering.
- Added an executable monotone-chain bound check (`--max-chain-bound`) to the toy script and refreshed the phase-2 artifact with `max_observed_chain=2`.
- Added [`scripts/check_timing_regression.py`](/home/c/Documents/code/FRACDASH/scripts/check_timing_regression.py) as a dedicated CPU timing-regression gate for future matrix runs.
- Captured the phase-2 toy artifact to [`benchmarks/results/2026-03-13-toy-dashi-phase2.json`](/home/c/Documents/code/FRACDASH/benchmarks/results/2026-03-13-toy-dashi-phase2.json) and validated the encode/decode round-trip across all 81 signed encodings.
- Added [`scripts/run_toy_dashi_phase2.sh`](/home/c/Documents/code/FRACDASH/scripts/run_toy_dashi_phase2.sh) to freeze a canonical artifact with default full-space chain bound `2`; added `--disable-chain-bound` support for exploratory generation.
- Updated `scripts/agdas_bridge.py` into an executable AGDAS bridge stage that emits a typed transition IR (with provenance) from comment markers and can emit FRACTRAN-form candidates when rules are present.
- Switched the AGDAS bridge to scan the real `.agda` source tree in `../dashi_agda` by default; the local `all_dashi_agdas.txt` mirror is no longer the primary input.
- Pivoted the AGDAS bridge direction so FRACDASH-side mapping/templates are the primary execution path, with `../dashi_agda` used as semantic reference rather than as a required annotated source.
- Added `AGDAS_BRIDGE_MAPPING.md`, wave-1 bridge templates in `scripts/agdas_bridge.py`, template execution support in `scripts/toy_dashi_transitions.py --agdas-templates`, and captured the first template artifact in `benchmarks/results/2026-03-14-agdas-template-phase2.json`.
- Added a selectable template-set mechanism (`wave1`, `wave2`, `all`) to the AGDAS bridge and verifier path.
- Added a compressed wave-2 `MonsterState` / `Monster.Step` template mapping, validated it through the verifier, and captured `benchmarks/results/2026-03-14-agdas-template-wave2-phase2.json`; the result is mostly terminal, which suggests the current 4-register encoding is too lossy for meaningful Monster-style dynamics.
- Pulled the canonical chat context for `69b35d79-1d90-839f-a358-5a26949aebd2` into `~/chat_archive.sqlite` and refreshed `COMPACTIFIED_CONTEXT.md`; the relevant sharpened point is that independent rank-4 diagnostics reinforce preserving low-dimensional structure rather than collapsing the bridge to trivial dynamics.
- Added `scripts/agdas_wave3_state.py` as the first enlarged carrier prototype (`3^6 = 729` states) for wave-3 AGDAS/Monster bridge work and validated its encode/decode round-trip.
- Captured the wave-3 carrier reference artifact at `benchmarks/results/2026-03-14-agdas-wave3-state.json`.
- Added a wave-3 Monster-facing template set to `scripts/agdas_bridge.py`, a dedicated runner in `scripts/agdas_wave3_experiments.py`, and the stable artifact `benchmarks/results/2026-03-14-agdas-template-wave3-phase2.json`.
- The wave-3 comparison confirms the larger carrier is wired correctly, but it is still a one-step surface: it expands from 81 to 729 states and from 18 to 54 edges relative to wave-2, yet longest chain remains `1` because no transition currently refreshes pending choice/admissibility state after a step.
- Added `formalism/PhysicsBridgeRefreshSketch.agda` as a local-only formal design note rather than touching upstream AGDA sources.
- Added the `physics1` bridge templates to `scripts/agdas_bridge.py`, validated them through `scripts/toy_dashi_transitions.py`, and captured `benchmarks/results/2026-03-14-agdas-physics1-phase2.json`.
- The `physics1` seam is already more informative than the Monster probe on the current carrier: it has 81 edges over 81 states, 48 cycles, and longest observed fixed-prime walk length `5`, which makes it the higher-signal next path for James-facing work.
- Implemented the widened `physics2` layer in one pass: added `scripts/agdas_physics2_state.py`, added the `physics2` template set to `scripts/agdas_bridge.py`, added the dedicated runner `scripts/agdas_physics_experiments.py`, and captured `benchmarks/results/2026-03-14-agdas-physics2-phase2.json`.
- The first `physics2` result is materially stronger than `physics1`: the 6-register carrier produces a graph with 657 edges over 729 states, longest chain `12`, 202 cyclic starts, and a canonical 4-step scan -> join -> contract -> rearm cycle.
- Implemented the `physics3` cone-shell refinement on the same 6-register carrier: added the `physics3` template set to `scripts/agdas_bridge.py`, extended `scripts/agdas_physics_experiments.py` with action-rank monotonicity reporting, and added `scripts/run_agdas_physics3_phase2.sh`.
- Captured `benchmarks/results/2026-03-14-agdas-physics3-phase2.json`; it widens the recurrent graph to 900 edges over 729 states, raises longest chain to `15`, and makes the canonical deterministic loop explicitly 5-step: scan -> join -> contract -> boundary_to_shell -> shell_to_interior.
- The new action monotonicity summary is mixed rather than trivially monotone: across 900 edges the first pass reports 234 decreases, 504 preserves, and 162 increases, which means the next physics pass should decide whether to refine scan structure further or strengthen the action-order constraint itself.
- Took the next pass as `physics4` and kept the same 6-register carrier: tightened scan/rearm guards in `scripts/agdas_bridge.py`, extended `scripts/agdas_physics_experiments.py` to compare against the `physics3` artifact, and added `scripts/run_agdas_physics4_phase2.sh`.
- Captured `benchmarks/results/2026-03-14-agdas-physics4-phase2.json`; it cuts the action monotonicity increases from `162` to `9`, but also collapses the graph to `162` edges, longest chain `9`, and zero cycles, so the stricter guard discipline is informative but overconstrained.
- The current leading interpretation is now sharper: `physics3` is still the best recurrent physics seam, while `physics4` isolates the part of the reset logic that is too strict. The next pass should be a hybrid that recovers recurrence without reopening the full `physics3` increase surface.
- Implemented the `physics5` hybrid on the same 6-register carrier: kept the `physics4` scan/boundary discipline, split shell-to-interior rearm into cleared and latched-left/right variants in `scripts/agdas_bridge.py`, extended `scripts/agdas_physics_experiments.py` to compare directly against both `physics3` and `physics4`, and added `scripts/run_agdas_physics5_phase2.sh`.
- Captured `benchmarks/results/2026-03-14-agdas-physics5-phase2.json`; it restores recurrence with `100` cyclic starts and a cyclic deterministic walk, while keeping action-rank increases to `27` instead of the `162` seen in `physics3`.
- The tradeoff is now much clearer: `physics5` is the best ordered hybrid so far, but it does not recover the richer `physics3` graph (`180` edges and longest chain `10` versus `900` edges and `15`), so the next pass should widen recurrence from the `physics5` core rather than loosen the bridge all the way back to `physics3`.
- Implemented `physics6` as the next narrow recurrence-widening pass: kept the `physics5` core, added left/right shell-refresh transitions in `scripts/agdas_bridge.py`, extended `scripts/agdas_physics_experiments.py` to compare against `physics5`, and added `scripts/run_agdas_physics6_phase2.sh`.
- Captured `benchmarks/results/2026-03-14-agdas-physics6-phase2.json`; it improves the ordered hybrid again with `198` edges, longest chain `12`, `115` cyclic starts, and the same `27` action-rank increases as `physics5`.
- Added built-in long-tail diagnosis to `scripts/agdas_physics_experiments.py` so the artifact now reruns any apparent timeout states at a higher cap. For `physics6`, the lone state that timed out at `12` resolves to a cycle by `32`, which removes tail instability as the immediate concern.
- The canonical deterministic walk remains a 6-step cycle with explicit shell refresh, and the next pass should now focus on widening recurrence from the `physics6` baseline rather than chasing timeout ambiguity.
- Implemented `physics7` on top of `physics6`: added preserve-only shell probe transitions for cleared shell states in `scripts/agdas_bridge.py`, extended the experiment runner to execute `physics7`, and added `scripts/run_agdas_physics7_phase2.sh`.
- Captured `benchmarks/results/2026-03-14-agdas-physics7-phase2.json`; it improves breadth over `physics6` (`204` edges vs `198`, `116` cyclic starts vs `115`) while keeping action-rank increases flat at `27`.
- Longest chain remains `12`, so the current constraint-preserving improvements are breadth-first. Next pass should target depth improvements without increasing jump count.
- Implemented `physics8` on top of `physics7`: added staged shell probe/release transitions in `scripts/agdas_bridge.py`, extended the experiment runner to execute `physics8` and compare against `physics7`, and added `scripts/run_agdas_physics8_phase2.sh`.
- Captured `benchmarks/results/2026-03-14-agdas-physics8-phase2.json`; it improves both breadth and depth over `physics7` (`222` edges vs `204`, longest chain `13` vs `12`, `132` cyclic starts vs `116`) while keeping action-rank increases flat at `27` and timeout count at `0` for `max-steps 12`.
- `physics8` is now the strongest constrained hybrid baseline; the next pass should continue from this shape without relaxing the jump-count or timeout constraints.
